<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowernotes.api.dao.UserMapper">
	
	
    <select id="listPage" parameterType="UserBean" resultType="UserBean">
    </select>

    <select id="get" parameterType="UserBean" resultType="UserBean">
	</select>
    
    <delete id="delete" parameterType="UserBean">
    </delete>
 
    <insert id="add" parameterType="UserBean" keyProperty="id" useGeneratedKeys="true">
	</insert>
    
    <update id="update" parameterType="UserBean">
    </update>

    <select id="select" parameterType="UserBean" resultType="UserBean">
    </select>

    <select id="listPageDesc" parameterType="UserBean" resultType="UserBean">
    </select>

</mapper>