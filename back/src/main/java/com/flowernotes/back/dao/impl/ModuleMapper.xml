<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowernotes.back.dao.ModuleMapper">
	
	
    <select id="listPage" parameterType="ModuleBean" resultType="ModuleBean">
    </select>

    <select id="get" parameterType="ModuleBean" resultType="ModuleBean">
	</select>
    
    <delete id="delete" parameterType="ModuleBean">
    </delete>
 
    <insert id="add" parameterType="ModuleBean" keyProperty="id" useGeneratedKeys="true">
	</insert>
    
    <update id="update" parameterType="ModuleBean">
    </update>

    <select id="select" parameterType="ModuleBean" resultType="ModuleBean">
    </select>

    <select id="listPageDesc" parameterType="ModuleBean" resultType="ModuleBean">
    </select>

</mapper>