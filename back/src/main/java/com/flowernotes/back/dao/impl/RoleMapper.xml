<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowernotes.back.dao.RoleMapper">
	
	
    <select id="listPage" parameterType="RoleBean" resultType="RoleBean">
    </select>

    <select id="get" parameterType="RoleBean" resultType="RoleBean">
	</select>
    
    <delete id="delete" parameterType="RoleBean">
    </delete>
 
    <insert id="add" parameterType="RoleBean" keyProperty="id" useGeneratedKeys="true">
	</insert>
    
    <update id="update" parameterType="RoleBean">
    </update>

    <select id="select" parameterType="RoleBean" resultType="RoleBean">
    </select>

    <select id="listPageDesc" parameterType="RoleBean" resultType="RoleBean">
    </select>

</mapper>